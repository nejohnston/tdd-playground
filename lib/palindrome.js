module.exports = (isPalindrome) => {
  //   const givenPalindrome = isPalindrome.toLowerCase();
  //   const givenPalindromeNoSpaces = givenPalindrome.replace(' ', '');
  //   const palindromeArray = givenPalindromeNoSpaces.map('');
  //   const givenFilteredPalindrome = givenPalindrome.filter('');
  if (typeof isPalindrome !== 'string' || isPalindrome !== undefined) {
    return undefined;
  }
  return false;
};
